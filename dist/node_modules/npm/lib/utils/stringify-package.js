'use strict';

module.exports = stringifyPackage;

var DEFAULT_INDENT = 2;
var CRLF = '\r\n';
var LF = '\n';

function stringifyPackage(data, indent, newline) {
  var json = JSON.stringify(data, null, indent || DEFAULT_INDENT);

  if (newline === CRLF) {
    return json.replace(/\n/g, CRLF) + CRLF;
  }

  return json + LF;
}
//# sourceMappingURL=stringify-package.js.map