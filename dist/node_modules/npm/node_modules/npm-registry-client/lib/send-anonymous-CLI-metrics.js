'use strict';

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

module.exports = send;

var assert = require('assert');
var url = require('url');

function send(registryUrl, params, cb) {
  assert(typeof registryUrl === 'string', 'must pass registry URI');
  assert(params && (typeof params === 'undefined' ? 'undefined' : _typeof(params)) === 'object', 'must pass params');
  assert(typeof cb === 'function', 'must pass callback');

  var uri = url.resolve(registryUrl, '-/npm/anon-metrics/v1/' + encodeURIComponent(params.metricId));

  this.request(uri, {
    method: 'PUT',
    body: JSON.stringify(params.metrics),
    authed: false
  }, cb);
}
//# sourceMappingURL=send-anonymous-CLI-metrics.js.map