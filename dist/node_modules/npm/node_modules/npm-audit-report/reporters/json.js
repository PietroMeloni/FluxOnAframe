'use strict';

var report = function report(data, options) {
  var defaults = {
    indent: 2
  };

  var config = Object.assign({}, defaults, options);

  var json = JSON.stringify(data, null, config.indent);
  return {
    report: json,
    exitCode: 0
  };
};

module.exports = report;
//# sourceMappingURL=json.js.map