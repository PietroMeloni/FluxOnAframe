'use strict';

var Utils = require('../lib/utils');

var report = function report(data) {
  var totalVulnCount = Utils.totalVulnCount(data.metadata.vulnerabilities);

  return {
    report: '',
    exitCode: totalVulnCount === 0 ? 0 : 1
  };
};

module.exports = report;
//# sourceMappingURL=quiet.js.map