'use strict';

var reporters = {
  install: require('./reporters/install'),
  parseable: require('./reporters/parseable'),
  detail: require('./reporters/detail'),
  json: require('./reporters/json'),
  quiet: require('./reporters/quiet')
};

var report = function report(data, options) {
  var defaults = {
    reporter: 'install',
    withColor: true,
    withUnicode: true
  };

  var config = Object.assign({}, defaults, options);
  return new Promise(function (resolve) {
    var result = reporters[config.reporter](data, config);
    return resolve(result);
  });
};

module.exports = report;
//# sourceMappingURL=index.js.map