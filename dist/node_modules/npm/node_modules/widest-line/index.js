'use strict';

var stringWidth = require('string-width');

module.exports = function (input) {
  return Math.max.apply(null, input.split('\n').map(function (x) {
    return stringWidth(x);
  }));
};
//# sourceMappingURL=index.js.map