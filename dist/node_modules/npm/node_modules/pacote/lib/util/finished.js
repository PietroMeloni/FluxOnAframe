'use strict';

var BB = require('bluebird');

module.exports = function (child) {
  var hasExitCode = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;

  return BB.fromNode(function (cb) {
    child.on('error', cb);
    child.on(hasExitCode ? 'close' : 'end', function (exitCode) {
      if (exitCode === undefined || exitCode === 0) {
        cb();
      } else {
        var err = new Error('exited with error code: ' + exitCode);
        cb(err);
      }
    });
  });
};
//# sourceMappingURL=finished.js.map