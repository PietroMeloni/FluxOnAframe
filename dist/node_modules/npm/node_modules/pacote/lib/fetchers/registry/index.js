'use strict';

var cacache = require('cacache');
var Fetcher = require('../../fetch');
var regManifest = require('./manifest');
var regTarball = require('./tarball');

var fetchRegistry = module.exports = Object.create(null);

Fetcher.impl(fetchRegistry, {
  manifest: function manifest(spec, opts) {
    return regManifest(spec, opts);
  },
  tarball: function tarball(spec, opts) {
    return regTarball(spec, opts);
  },
  fromManifest: function fromManifest(manifest, spec, opts) {
    return regTarball.fromManifest(manifest, spec, opts);
  },
  clearMemoized: function clearMemoized() {
    cacache.clearMemoized();
    regManifest.clearMemoized();
  }
});
//# sourceMappingURL=index.js.map