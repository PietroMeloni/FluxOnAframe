#!/usr/bin/env node
'use strict';

var npx = require('libnpx');
var path = require('path');

var NPM_PATH = path.join(__dirname, 'npm-cli.js');

npx(npx.parseArgs(process.argv, NPM_PATH));
//# sourceMappingURL=npx-cli.js.map